#include <bits/stdc++.h>
#define FAST ios_base::sync_with_stdio(false);cin.tie(NULL);
#define int long long
using namespace std;

signed main()
{
    FAST;
    int n, m;
    cin >> n >> m;
    vector<int> a, b;
    for(int i=0; i<n; i++)
    {
        cin >> num;
        a.push_back(num);
    }
    for(int i=0; i<m; i++)
    {
        cin >> num;
        b.push_back(num);
    }

	return 0;
}
